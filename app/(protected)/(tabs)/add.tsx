import React from 'react';
import { View } from 'react-native';

// This file exists only to make the tab appear in the tab bar
// The actual navigation happens through the tab press listener in _layout.tsx
export default function AddTab() {
  return <View />;
} 